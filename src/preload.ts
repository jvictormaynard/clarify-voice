// Not strictly needed since we enabled nodeIntegration for simplicity in this small tool,
// but good practice to have the file.
window.addEventListener('DOMContentLoaded', () => {
    // Preload logic if needed
  })
